#include <stdio.h>
#include <stdlib.h>
#include <time.h>
//Zadanie 4 - Napisz program znajdujący minimalną wartość w tablicy n-elementowej, elementy tablicy
// są losowane z przedziału[-23, 50].

int main()
{
	int n;

	printf("Podaj ilosc elementow twojej tablicy:\n ");
	scanf_s("%d", &n);
	int tab[n];

	int max = 50;

	srand(time, NULL);

	for(int i = -23; i < )


	return 0;
}